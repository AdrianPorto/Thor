{
    "version" : 2,
    "builds": [
        {
            "src": "/api/status",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/status",
            "dest": "/"
        }
    ]
}